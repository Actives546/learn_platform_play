<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learn.auth.mapper.MenuMapper">

    <resultMap id="BaseResultMap" type="com.learn.common.entity.Menu">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="menu_name" property="menuName"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="icon" property="icon"/>
        <result column="menu_type" property="menuType"/>
        <result column="perms" property="perms"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, parent_id, menu_name, path, component, icon, menu_type, perms, sort, status, 
        create_time, update_time
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM menu
        ORDER BY sort ASC, create_time ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM menu
        WHERE id = #{id}
    </select>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM menu
        WHERE parent_id = #{parentId}
        ORDER BY sort ASC, create_time ASC
    </select>

    <insert id="insert" parameterType="com.learn.common.entity.Menu" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO menu (
            id, parent_id, menu_name, path, component, icon, menu_type, perms, sort, status, 
            create_time, update_time
        ) VALUES (
            #{id}, #{parentId}, #{menuName}, #{path}, #{component}, #{icon}, #{menuType}, #{perms}, #{sort}, #{status},
            #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="com.learn.common.entity.Menu">
        UPDATE menu
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="menuName != null">menu_name = #{menuName},</if>
            <if test="path != null">path = #{path},</if>
            <if test="component != null">component = #{component},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="menuType != null">menu_type = #{menuType},</if>
            <if test="perms != null">perms = #{perms},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM menu
        WHERE id = #{id}
    </delete>

    <select id="countByParentId" resultType="int">
        SELECT COUNT(*)
        FROM menu
        WHERE parent_id = #{parentId}
    </select>

</mapper>
