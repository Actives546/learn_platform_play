<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learn.course.mapper.CourseMapper">

    <resultMap id="BaseResultMap" type="com.learn.common.entity.Course">
        <id column="id" property="id"/>
        <result column="course_name" property="courseName"/>
        <result column="cover" property="cover"/>
        <result column="description" property="description"/>
        <result column="teacher_id" property="teacherId"/>
        <result column="lesson_count" property="lessonCount"/>
        <result column="status" property="status"/>
        <result column="sort" property="sort"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, course_name, cover, description, teacher_id, lesson_count, status, sort, create_time, update_time, is_deleted
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM course
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM course
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND is_deleted = 0
    </select>

    <insert id="insert" parameterType="com.learn.common.entity.Course" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO course (
            id, course_name, cover, description, teacher_id, lesson_count, status, sort, create_time, update_time, is_deleted
        ) VALUES (
            #{id}, #{courseName}, #{cover}, #{description}, #{teacherId}, #{lessonCount}, #{status}, #{sort}, #{createTime}, #{updateTime}, 0
        )
    </insert>

    <update id="updateById" parameterType="com.learn.common.entity.Course">
        UPDATE course
        <set>
            <if test="courseName != null">course_name = #{courseName},</if>
            <if test="cover != null">cover = #{cover},</if>
            <if test="description != null">description = #{description},</if>
            <if test="teacherId != null">teacher_id = #{teacherId},</if>
            <if test="lessonCount != null">lesson_count = #{lessonCount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE course
        SET is_deleted = 1,
            update_time = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <select id="countTotal" resultType="long">
        SELECT COUNT(*)
        FROM course
        WHERE is_deleted = 0
        <if test="courseName != null and courseName != ''">
            AND course_name LIKE CONCAT('%', #{courseName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM course
        WHERE is_deleted = 0
        <if test="courseName != null and courseName != ''">
            AND course_name LIKE CONCAT('%', #{courseName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort ASC, create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <update id="updateStatusBatch">
        UPDATE course
        SET status = #{status},
            update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND is_deleted = 0
    </update>

    <select id="countTeachers" resultType="long">
        SELECT COUNT(DISTINCT teacher_id)
        FROM course
        WHERE is_deleted = 0
        AND teacher_id IS NOT NULL
    </select>

    <select id="countThisMonthNewCourses" resultType="long">
        SELECT COUNT(*)
        FROM course
        WHERE is_deleted = 0
        AND YEAR(create_time) = YEAR(CURRENT_DATE())
        AND MONTH(create_time) = MONTH(CURRENT_DATE())
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*)
        FROM course
        WHERE is_deleted = 0
        AND status = #{status}
    </select>

</mapper>
