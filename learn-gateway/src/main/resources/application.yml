server:
  port: 8080

spring:
  application:
    name: learn-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 198.18.0.1:8848
        namespace: public
        username: nacos
        password: nacos
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: learn-auth
          uri: lb://learn-auth
          predicates:
            - Path=/auth/**
          filters:
            - StripPrefix=1
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: 123456
      database: 0
      timeout: 10000ms

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.learn: debug
    org.springframework.cloud.gateway: debug
